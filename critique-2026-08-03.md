# StudyBuddy OnDemand — Critique Delta 2026-08-03 (v2.1)

**Baseline:** v2.0, 2026-07-27, HEAD `faadd787` (PR #519)
**Current HEAD:** `dae9b98` (nightly bot, 2026-08-03) — no human commits since 2026-07-25
**Window:** 0 human PRs; 8 nightly bot `docs/progress.json` commits only
**Theme:** Quiet maintenance window. One P1 resolved (credential rotation confirmed). No code changes, no schema changes, no new features.

Read alongside `studybuddy-critique.md` (living critique) and `critique-2026-07-27.md` (v2.0 base). This file records only what **changed** since v2.0.

---

## Numbers update

No code changes this window. All metrics carry forward from v2.0 unchanged.

| Metric | v2.0 (2026-07-27) | Now (2026-08-03) |
|---|---|---|
| Alembic migrations | 61 (latest 0061) | **61 — unchanged** |
| Backend tests | ~1,085 across 77 files | **unchanged** |
| Playwright specs | 17 files / 2,779 LOC | **unchanged** |
| GitHub Actions workflows | 9 | **unchanged** |
| Human commits this window | — | **0** |

---

## Resolved since v2.0

### ✅ RESOLVED — Live demo super-admin credential rotation confirmed complete (issue #517, closed 2026-07-25)

The P1 item flagged as the most urgent action in v2.0 ("rotate `Admin1234!` on `demo.usestudybuddy.com`") is **closed**. Issue #517 was filed and closed as `completed` on the same day as PR #516 (2026-07-25), indicating the operational rotation was performed promptly alongside the code fix.

Specifically closed:
- Old default `Admin1234!` no longer authenticates on `demo.usestudybuddy.com`
- `seed_super_admin.py` now fails-closed (no default; rejects weak values)
- `seed.sh` generates a strong random password on unset `SUPER_ADMIN_PASSWORD`

**Remaining follow-up (P2, unchanged):** The issue notes that `vm-localhost-bootstrap.sh` still contains dev-only hardcoded defaults (`DevAdmin1234!`). Lower severity (localhost only), not blocking.

---

## Still-open items (no change from v2.0)

### P1

| Item | Status |
|---|---|
| `APP_ENV ∈ {"development","staging","production"}` startup assertion | Unchanged — demo is live, this remains the most actionable hardening item |
| Consolidate rate limiting: remove slowapi in-process, Redis-backed `ip_auth_rate_limit` only | Unchanged |
| `DATABASE_POOL_MAX × WORKER_COUNT` → hard startup assertion | Unchanged |
| E2E functional coverage: 6 fixme'd school-admin curriculum specs (issue #188) | Unchanged |
| 3 disabled axe rules: `color-contrast`, `html-has-lang`, `document-title` (issue #189) | Unchanged |

### P2

- `vm-localhost-bootstrap.sh` hardcoded dev defaults (`DevAdmin1234!`) — localhost only, low severity
- Kivy quiz client broken: no `correct_option` in payload since server-side grading shipped (#506)
- Load tests absent
- Mobile/web capability boundary doc missing before Epic 3 Path B
- L-6 TTL sweeper paused (archived curricula accumulate)
- E2E coverage gaps: Authoring Studio flow, onboarding wizard (#420), Administration menu (#415/#417)
- ADR-005 status: **Proposed** → should reach **Accepted** once soft-delete account flow verified
- `purge_account.py` hard-gated as test-only — verify cannot run against production data

### P3

- Pass-rate denominator label discrepancy (Overview vs. At-Risk screens)
- Makefile absent
- Runbooks absent (DB exhaustion, Redis OOM, Stripe webhook backlog, Beat lock expiry)
- Alembic downgrade procedure undocumented (61 migrations)
- API deprecation policy unwritten

---

## Window summary

Quiet week — no human code changes since PR #519 landed 2026-07-25. The one notable update is confirmation that the credential rotation flagged as urgent in v2.0 was completed promptly (issue #517 closed same day as the PR). This removes the highest-urgency action. The remaining P1s — `APP_ENV` assertion, rate-limit consolidation, pool arithmetic guard, E2E functional coverage, and the 3 axe violations — have carried forward since the v1.7 baseline (2026-06-09) without a commit touching any of them. With the demo live and no active sprint visible in the commit log, these are the natural targets for the next focused session.

---

_Generated by [Claude Code](https://claude.ai/code)_
