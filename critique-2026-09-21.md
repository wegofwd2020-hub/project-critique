# StudyBuddy OnDemand — Critique Delta 2026-09-21 (v2.8)

**Baseline:** v2.7, 2026-09-14 / last substantive commit `568ceb1` (#752, 2026-09-11)
**Current HEAD (origin/main):** `279aa25` (nightly bot, 2026-09-21); last substantive commit **unchanged — still `568ceb1`**
**Window:** 0 substantive commits on `origin/main`; 10 nightly progress-chart commits only; 2026-09-11 → 2026-09-21
**Theme:** Quiet window on `main` — no code changes merged since last week. A significant feature branch is in flight (quiz answer validation + school localization + STEAM/EH report grouping + engagement report rebuild, ≥20 substantive commits) but not yet on `origin/main`.

Read alongside `studybuddy-critique.md` (living critique v1.8) and `critique-2026-09-14.md` (v2.7 base). This file records only **what changed** since v2.7.

---

## Numbers update

| Metric | v2.7 (2026-09-14) | Now (2026-09-21) |
|---|---|---|
| Backend tests (grep) | ≈1,514 functions, 140 files | **≈1,514 — unchanged** |
| Alembic migrations (origin/main) | 71 (latest 0071, `inactive_student_alerts`) | **71 — unchanged** |
| Substantive commits this window | — | **0 on origin/main** |
| ADRs | 8 | **8 — unchanged** |
| Playwright e2e specs | 19 files | **19 files — unchanged** |

---

## Resolved since v2.7

Nothing resolved. No merges to `origin/main` this window.

---

## What changed

### ⚠️ NOTE — Working tree is a detached HEAD on a feature branch

The repo on disk is checked out at `6c22097` (feature branch), **not** `origin/main` (`279aa25`). Per the project CLAUDE.md discipline ("Measure products against `origin/main`, not the working tree — checkouts drift"), numbers above are from `origin/main`. The feature branch contains at least the following work not yet on main:

| Commit | Change |
|---|---|
| `3a11335` Merge PR #828 `feat/eh-004-grade-steam-grouping` | Grouped report schemas + utilities for STEAM/EH stream grouping in the teacher engagement report |
| `7c8f951` fix(reports) #807 | Rebuild engagement report on the dashboard's vocabulary (#770) |
| `31381f2` feat(school) #762 (#809) | **Migration 0072 `question_validations`** — per-school reviewed-answer store, keyed by `stable_question_id` (ADR-008 / migration 0067). A school reviewer can record the correct answer text for a question per school. RLS-scoped. Downgrade: DROP TABLE CASCADE. |
| `6c22097` fix(#826) #832 | **Migration 0073 `school_localization`** — `school_localization` table with `country_code` (ISO 3166-1), `currency_code` (ISO 4217), `currency_symbol`, thousands/decimal separator, IANA timezone. FK CASCADE on `schools.school_id`. Motivation in commit: consistent decimal precision for average score display requires knowing the school's locale. |

**Assessment of in-flight work:** Migrations 0072/0073 are additive (new tables, FK+RLS, DROP on downgrade — no ALTER to existing tables), so the merge risk is low. The `question_validations` table completes the ADR-008 Phase 3 picture: stable_question_id (0067) → feedback grain (0068) → question registry (0069) → per-school validated answers (0072). `school_localization` is a new capability not foreshadowed in any ADR; worth a retro-ADR or at least a CHANGES.md entry on merge (ADR-006 pattern).

---

### No other changes this window

No new CVEs, no new tests, no doc changes, no dependency bumps on `origin/main` this week.

---

## Still open from v2.7 (all unchanged)

| Item | Status |
|---|---|
| Epic 2 hosting decision (G-1, G-4 blocked) | ⚠️ **P1 — no movement; 6+ windows running** |
| `purge_account.py` — comment-only gate, no env/hostname assertion | ⚠️ **P1 — 5 windows; still no code gate** |
| `APP_ENV` not asserted against valid enum at startup | ⚠️ P1 — unchanged |
| slowapi in-process + Redis-backed limiter coexistence | ⚠️ P1 — unchanged |
| `DATABASE_POOL_MAX` arithmetic warn-not-assert | ⚠️ P1 — unchanged |
| #188: school-admin curriculum flow — **5 `test.fixme` assertions** | ⚠️ P1 — unchanged |
| `color-contrast` axe rule still disabled | ⚠️ P1 — unchanged |
| `pricing.ts:180` stale comment on live independent-teacher plans | ⚠️ P2 — unchanged |
| ADR-007 (academic calendar) — still Proposed | ⚠️ P2 — unchanged |
| ADR-008 (delivery calibration) — still Proposed; Phase 3b pending | ⚠️ P2 — unchanged |
| `DOCS_REPO_TOKEN` not configured — external doc/glossary drift checks failing | ⚠️ P2 — unchanged |
| No load/performance tests | ⚠️ P2 — unchanged |
| Onboarding wizard (#420) + Administration menu (#415/#417) — E2E untested | ⚠️ P2 — unchanged |
| No `CONTRIBUTING.md` | ⚠️ P3 — unchanged |
| Pool arithmetic warn-not-assert | ⚠️ P1 — unchanged |

---

## Priority actions this week (delta only)

| Priority | Action |
|---|---|
| **P2** | Before merging the in-flight branch, confirm migration 0073 (`school_localization`) has a covering ADR or CHANGES.md entry — `school_localization` is a new tenant capability that appeared with no decision record; ADR-006 pattern suggests a retro-doc before main absorbs it |
| **P2** | Verify `question_validations` (0072) is covered by E2E or quiz-suite tests before merge — the ADR-008 Phase 3 chain (0067→0068→0069→0072) has strong unit coverage but the end-to-end "reviewer marks answer → student sees corrected feedback" path has not appeared in any Playwright spec |
| **P2** (standing) | Epic 2 hosting decision — 6 consecutive quiet windows on this item; now the longest-running open P1 with no commits approaching it |
